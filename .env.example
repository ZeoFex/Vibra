# Copy to .env.local and set your own values (never commit .env.local)

# Shared demo password for local admin login (server-side only, not exposed to the browser bundle)
VIBRA_ADMIN_DEMO_PASSWORD=change-me-local-only

# Allowed demo admin emails (configured in lib/mock-data/admin/index.ts):
# - admin@vibra.app       (Super Admin)
# - verify@vibra.app      (Verification Manager)
